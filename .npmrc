# npm reads this during `changeset publish`. pnpm settings are in package.json → "pnpm".
@loom-mvvm:registry=https://registry.npmjs.org/
//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}
access=public
