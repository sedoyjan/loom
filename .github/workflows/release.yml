# Disabled — publish manually with `pnpm release` after `npm login` (see docs/releases.md).
# Re-enable push + changesets/action when @loom npm scope and Trusted Publishing are ready.

name: Release

on:
  workflow_dispatch:

jobs:
  release:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow is disabled."
